# Local Setup

> _Placeholder — replace with real setup steps for your stack._

## Prerequisites

- List required tools and versions here (languages, runtimes, CLIs).

## Setup

```bash
# Clone and install
git clone git@github.com:claimsync/<repo>.git
cd <repo>
# install dependencies...
```

## Run the docs site locally

```bash
pip install -r requirements.txt
mkdocs serve   # http://127.0.0.1:8000
```
